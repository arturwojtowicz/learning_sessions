# Numbers
num_integer = 2
num_float = 1.7
num_complex = 2 + 3j

# Text
text_str = "Hello Capgemini!"

# Boolean
bool_bool = True

# Sequential
seq_list = [9, 4, "Hello", "ABC", 5.849]
seq_tuple = (9, 4, "Hello", "ABC", 5.849)
seq_range = range(10)

# Sets
set_set = set(seq_list)

# Mapping
map_dict = {
    "Hello": "Capgemini!",
    1: 1.0,
    "ref": {
        "stuff": 0
    }
}

# 1. Checking types
# 2. What does it mean that variable has a type?
# 3. Allocating memory as id()
#   3.1 mutable - list, dict, set, byte array
#   3.2 immutable - int, float, complex, string, tuple, frozenset, bytes
# 4. Operations
# 5. Extra:
#   5.1 List Comprehension
#   5.2 Generators
#   5.3 Dict Comprehension?

import pdb;pdb.set_trace()